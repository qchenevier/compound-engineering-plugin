# fixture: review-all-deadline

`run/` is a ce-code-review run directory at scope `all` during collection, after Stage 4's local wave returned. Six external jobs were started; none has a status file or an artifact, so all six are still running.
