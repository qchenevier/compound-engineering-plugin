# fixture: review-all-one-failed

`run/` is a ce-code-review run directory at scope `all` after Stage 4's local wave returned. Correctness and performance completed with artifacts. The security job ended `failed` with no artifact; its `out.log` carries the worker's evidence.
