# fixture: review-all-quota

`run/` is a ce-code-review run directory at scope `all` during collection, after Stage 4's local wave returned. Maintainability completed with an artifact. The correctness job ended `failed`; its `out.log` carries the worker's evidence. The security and performance jobs have no status file yet, so they are still running. The shared deadline has not passed.
