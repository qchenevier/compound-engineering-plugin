# fixture: review-all-folded

`run/` is a ce-code-review run directory at scope `all` after Stage 4's local wave returned. Three external jobs (correctness, security, performance) completed and their artifacts were written; the job directories were already consumed.
