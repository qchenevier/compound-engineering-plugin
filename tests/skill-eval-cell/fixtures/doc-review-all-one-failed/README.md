# fixture: doc-review-all-one-failed

A unified plan for a weekly usage digest, reviewed at cross-model scope `all`. `run/` is the ce-doc-review run directory after the host wave returned: the product-lens job completed with an artifact, and the coherence job ended `failed` with no artifact; its `out.log` carries the worker's evidence.
